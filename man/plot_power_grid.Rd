% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_power_grid}
\alias{plot_power_grid}
\title{Power grid heatmap — n plots × effort (or any two design axes)}
\usage{
plot_power_grid(
  results,
  y_var = "n_plots",
  x_var = "effect_pct",
  facet_var = NULL,
  group_filter = "All",
  y_lab = NULL,
  x_lab = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL
)
}
\arguments{
\item{results}{A \code{monpwr_results} data frame from \code{\link[=run_power_sim]{run_power_sim()}}, with
any additional design columns already attached.}

\item{y_var}{Character scalar.  Column to place on the y-axis.
Default \code{"n_plots"} (already present in all results).}

\item{x_var}{Character scalar.  Column to place on the x-axis.
Default \code{"effect_pct"}.}

\item{facet_var}{Character scalar or \code{NULL}.  Column to facet by.
\code{NULL} (default) produces a single panel.}

\item{group_filter}{Character scalar or \code{NULL}.  Group to include.
Default \code{"All"}.  Pass \code{NULL} to include all groups.}

\item{y_lab}{Character scalar.  Y-axis label.  Defaults to \code{y_var}.}

\item{x_lab}{Character scalar.  X-axis label.  Defaults to \code{x_var}.}

\item{title}{Character scalar.  Plot title.}

\item{subtitle}{Character scalar.  Plot subtitle.}

\item{caption}{Character scalar.  Plot caption.}
}
\value{
A \code{ggplot} object.
}
\description{
Tile heatmap showing power for every combination of two design variables,
optionally faceted by a third.  The natural use case is exploring how
sample size and survey effort interact: rows = n plots, columns = effort,
facets = effect size (or horizon).

Any column present in \code{results} (including columns added via \code{dplyr::mutate}
after \code{\link[=run_power_sim]{run_power_sim()}}, such as \code{effort_hrs}) can be mapped to any axis.
}
\seealso{
\code{\link[=run_power_sim]{run_power_sim()}}, \code{\link[=plot_power]{plot_power()}}, \code{\link[=plot_mdc]{plot_mdc()}}
}
