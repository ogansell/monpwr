% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialisers.R
\name{init_conditional}
\alias{init_conditional}
\title{Initialise legacy plot state for conditional simulation}
\usage{
init_conditional(ref_params, site_ids, ...)
}
\arguments{
\item{ref_params}{A \code{monpwr_params} object from \code{\link[=extract_params]{extract_params()}}.}

\item{site_ids}{Character vector of site IDs (matching the \code{Place} column
in \code{ref_params$plot_state}) for the retained plots under the current
scenario.}

\item{...}{Ignored; present for interface consistency.}
}
\value{
A data frame with one row per retained plot, containing:
\code{Place}, \code{plotid_model}, \code{visit_num}, \code{eta_last_cond}, \code{eta_last_zi},
\code{blup_cond}, \code{blup_zi}.
}
\description{
Builds the plot-state data frame for \strong{conditional} simulation by
filtering \code{ref_params$plot_state} to the retained site IDs.  Each plot
starts at its last observed visit number and fitted linear predictor,
so future simulated visits build on the existing monitoring record.

This is the default initialiser used by \code{\link[=run_power_sim]{run_power_sim()}} for legacy
plots when \code{mode = "conditional"} or in the legacy component of a hybrid
scenario.  You will not normally need to call it directly.
}
\seealso{
\code{\link[=init_new_sites]{init_new_sites()}}, \code{\link[=init_prospective_marginal]{init_prospective_marginal()}}, \code{\link[=run_power_sim]{run_power_sim()}}
}
