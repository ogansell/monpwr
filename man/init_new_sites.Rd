% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialisers.R
\name{init_new_sites}
\alias{init_new_sites}
\title{Initialise new sites for hybrid scenarios}
\usage{
init_new_sites(
  ref_params,
  n_new,
  eta_offset_cond = 0,
  eta_offset_zi = eta_offset_cond,
  id_prefix = ".new_",
  ...
)
}
\arguments{
\item{ref_params}{A \code{monpwr_params} object from \code{\link[=extract_params]{extract_params()}}.}

\item{n_new}{Integer scalar.  Number of new sites to initialise.}

\item{eta_offset_cond}{Numeric scalar.  Optional shift on the marginal
conditional intercept (log scale).  Use a positive value if new sites
are targeted at areas expected to be above the population average, or
negative for below-average areas.  Default \code{0}.}

\item{eta_offset_zi}{Numeric scalar.  Same shift for the ZI component.
Defaults to \code{eta_offset_cond}.}

\item{id_prefix}{Character scalar.  Prefix for synthetic site ID strings.
Default \code{".new_"}.  Should not clash with any real site IDs.}

\item{...}{Ignored; present for interface consistency.}
}
\value{
A data frame with one row per new site, with the same column
structure as \code{ref_params$plot_state} (\code{Place}, \code{plotid_model},
\code{visit_num}, \code{eta_last_cond}, \code{eta_last_zi}, \code{blup_cond}, \code{blup_zi}),
suitable for binding with \code{init_conditional()} output before passing to
\code{\link[=simulate_visits]{simulate_visits()}}.
}
\description{
Creates a synthetic plot-state data frame for \strong{new sites} that have no
existing monitoring record.  Each new site is initialised at \code{visit_num = 0}
with a linear predictor drawn from the population marginal distribution:
the marginal intercept (at \code{visit_num = 0}) plus a freshly sampled BLUP
from \eqn{N(0, \hat\sigma)}.

This function is called \strong{inside every simulation replicate} by
\code{\link[=run_power_sim]{run_power_sim()}} so that BLUPs are independently resampled each time.
Fixing BLUPs across replicates would understate new-site variability and
overstate power.
}
\details{
\subsection{When to use \code{eta_offset_cond}}{

By default new sites are assumed to be drawn from the same population as
existing sites.  If your new sites are targeted (e.g. gap-filling in areas
known to have lower occupancy), supply a negative offset to shift the
starting intercept accordingly.  The offset is on the linear-predictor
(log or logit) scale.
}

\subsection{Writing a custom new-site initialiser}{

If you need more control — for example, initialising new sites from a
specific covariate profile rather than the population marginal — write a
function with the signature:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{my_new_init <- function(ref_params, n_new, ...) \{
  # return a data.frame with columns:
  #   Place, plotid_model, visit_num (0L), eta_last_cond, eta_last_zi,
  #   blup_cond, blup_zi
\}
}\if{html}{\out{</div>}}

Pass it as \code{new_site_init_fn} to \code{\link[=scenario]{scenario()}}.
}
}
\seealso{
\code{\link[=init_conditional]{init_conditional()}}, \code{\link[=scenario]{scenario()}}, \code{\link[=run_power_sim]{run_power_sim()}}
}
