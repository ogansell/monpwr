% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summaries.R
\name{with_sigma_scaling}
\alias{with_sigma_scaling}
\title{Bracket variance-estimate uncertainty by scaling the random-effect SD}
\usage{
with_sigma_scaling(ref_params, scales = c(0.8, 1, 1.2), run_fn = NULL)
}
\arguments{
\item{ref_params}{A \code{monpwr_params} object from \code{\link[=extract_params]{extract_params()}}.}

\item{scales}{Numeric vector of strictly positive multipliers applied to
\code{sigma_cond} (and \code{sigma_zi} when it is \verb{> 0}).  Default \code{c(0.8, 1, 1.2)}.}

\item{run_fn}{Optional function of a single \code{monpwr_params} argument that
returns a \code{monpwr_results} data frame (e.g. a partially-applied
\code{\link[=run_power_sim]{run_power_sim()}}).  If supplied, the helper runs it for each scale and
row-binds the results with a numeric \code{sigma_scale} column.  If \code{NULL}
(default), the helper returns a named list of scaled \code{monpwr_params}
objects instead.}
}
\value{
If \code{run_fn} is \code{NULL}: a named list of \code{monpwr_params} objects, one
per scale (names are the scale values).  Otherwise: a single data frame —
the row-bound \code{run_fn} outputs with an added \code{sigma_scale} column.
}
\description{
Variance components (\code{sigma_cond}, \code{sigma_zi}) estimated from a small pilot
are uncertain, and power is sensitive to them.  This helper rescales the
random-effect SD of a \code{monpwr_params} object by one or more multipliers so
the user can bracket how much that uncertainty moves the power estimate —
operationalising the sensitivity-analysis guidance in the package
documentation.

Only the random-effect SD is scaled.  The dispersion parameter \code{disp_par}
is left unchanged, because it represents a different (observation-level)
source of uncertainty.
}
\seealso{
\code{\link[=calibrate_bias]{calibrate_bias()}}, \code{\link[=run_power_sim]{run_power_sim()}}, \code{\link[=extract_params]{extract_params()}}
}
