% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_power}
\alias{plot_power}
\title{Plot power curves by scenario}
\usage{
plot_power(
  results,
  group_filter = "All",
  sim_type_filter = "combined",
  power_target = 0.8,
  alpha = 0.1,
  title = NULL
)
}
\arguments{
\item{results}{A \code{monpwr_results} data frame from \code{\link[=run_power_sim]{run_power_sim()}}.}

\item{group_filter}{Character scalar or \code{NULL}.  Group to display.
Default \code{"All"} (all selected sites).  Pass \code{NULL} to include all groups.}

\item{sim_type_filter}{Character scalar.  Which simulation type to display:
\code{"combined"} (default) or \code{"legacy_only"}.}

\item{power_target}{Numeric scalar.  Reference line for target power.
Default 0.80.}

\item{alpha}{Numeric scalar.  Passed to the caption.  Default 0.10.}

\item{title}{Character scalar.  Plot title.  Default auto-generated.}
}
\value{
A \code{ggplot} object.
}
\description{
Bar chart of power by effect size, faceted by monitoring horizon.  One
panel per horizon, bars grouped by scenario.

For hybrid scenarios the output contains both \code{"combined"} (legacy + new
sites) and \code{"legacy_only"} rows.  Use \code{sim_type_filter} to select which
to display, or call \code{\link[=plot_power_gain]{plot_power_gain()}} to show both traces together.
}
\seealso{
\code{\link[=plot_power_gain]{plot_power_gain()}}, \code{\link[=plot_mdc]{plot_mdc()}}, \code{\link[=plot_cv]{plot_cv()}}, \code{\link[=run_power_sim]{run_power_sim()}}
}
