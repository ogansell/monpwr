% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summaries.R
\name{bootstrap_cv}
\alias{bootstrap_cv}
\title{Bootstrap coefficient of variation of mean response}
\usage{
bootstrap_cv(
  data,
  site_ids,
  n_boot = 1000L,
  place_var = "Place",
  season_var = "Season",
  response_fn = function(d) mean(d$count > 0)
)
}
\arguments{
\item{data}{A data frame — the modelling dataset, one row per site × visit.}

\item{site_ids}{Character vector.  Site IDs to include.}

\item{n_boot}{Integer scalar.  Bootstrap replicates.  Default 1000.}

\item{place_var}{Character scalar.  Site ID column in \code{data}.
Default \code{"Place"}.}

\item{season_var}{Character scalar.  Season/year column used to select
the most recent monitoring round.  Default \code{"Season"}.  If not found
in \code{data}, all rows for the retained sites are used.}

\item{response_fn}{A function applied to each site's rows (a data frame
subset) that returns a single numeric value representing that site's
state estimate.  Default: proportion of rows with \code{count > 0}
(\code{function(d) mean(d$count > 0)}).  Supply a custom function for
indices defined differently in your monitoring programme.}
}
\value{
A one-row data frame with columns: \code{n_sites}, \code{mean_resp}, \code{cv}.
Returns \code{NA} for \code{mean_resp} and \code{cv} if fewer than 2 sites have data.
}
\description{
Estimates the precision of a state estimate for a set of sites using a
nonparametric bootstrap over sites.  The CV (coefficient of variation of
the bootstrap distribution of the mean) is a common design-adequacy
criterion (typical threshold: CV < 0.20).

Only \strong{legacy} sites with an observed historical record contribute to the
CV.  New sites in hybrid scenarios have no observed data and should not be
included in \code{site_ids}.
}
\seealso{
\code{\link[=run_precision]{run_precision()}}, \code{\link[=plot_cv]{plot_cv()}}
}
