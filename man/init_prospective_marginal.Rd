% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialisers.R
\name{init_prospective_marginal}
\alias{init_prospective_marginal}
\title{Initialise plot state for prospective (from-scratch) simulation}
\usage{
init_prospective_marginal(ref_params, n_plots, ...)
}
\arguments{
\item{ref_params}{A \code{monpwr_params} object from \code{\link[=extract_params]{extract_params()}}.}

\item{n_plots}{Integer scalar.  Number of plots to simulate.}

\item{...}{Ignored; present for interface consistency with custom
\code{init_fn} functions.}
}
\value{
A data frame with one row per plot, containing:
\code{plotid} (character), \code{visit_num} (integer, \code{0L}),
\code{eta_last_cond} (numeric, marginal intercept),
\code{eta_last_zi} (numeric, marginal ZI intercept or \code{0}).
}
\description{
Builds a synthetic plot-state data frame for \strong{prospective} simulation.
Each plot is initialised at \code{visit_num = 0} with linear predictors set to
the \strong{marginal (population-average) prediction}.  Random effects are drawn
fresh each simulation replicate inside \code{\link[=simulate_visits]{simulate_visits()}}.

This represents "what power would we have if we were starting today with
no existing monitoring record" — equivalent to \code{simr}-style power analysis.

This is the default initialiser used by \code{\link[=run_power_sim]{run_power_sim()}} when
\code{mode = "prospective"}.
}
\details{
\subsection{Writing a custom \code{init_fn} for prospective mode}{

If you want to initialise prospective plots with specific covariate
profiles (e.g. sampled from your empirical data, or set to a design
target), supply your own function with the signature:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{my_init_fn <- function(ref_params, n_plots, ...) \{
  # return a data.frame with columns:
  #   plotid        <character>  unique plot identifier
  #   visit_num     <integer>    starting visit number (typically 0L)
  #   eta_last_cond <numeric>    starting linear predictor, conditional
  #   eta_last_zi   <numeric>    starting linear predictor, ZI (0 if no ZI)
\}

run_power_sim(..., mode = "prospective", init_fn = my_init_fn)
}\if{html}{\out{</div>}}
}
}
\seealso{
\code{\link[=init_conditional]{init_conditional()}}, \code{\link[=init_new_sites]{init_new_sites()}}, \code{\link[=run_power_sim]{run_power_sim()}}
}
