% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_power_gain}
\alias{plot_power_gain}
\title{Plot hybrid power gain - combined vs legacy-only traces}
\usage{
plot_power_gain(
  results,
  group_filter = "All",
  power_target = 0.8,
  title = NULL
)
}
\arguments{
\item{results}{A \code{monpwr_results} data frame from \code{\link[=run_power_sim]{run_power_sim()}}.}

\item{group_filter}{Character scalar or \code{NULL}.  Group to display.
Default \code{"All"}.  Pass \code{NULL} to include all groups.}

\item{power_target}{Numeric scalar.  Reference line.  Default 0.80.}

\item{title}{Character scalar.  Plot title.  Default auto-generated.}
}
\value{
A \code{ggplot} object, or \code{NULL} (invisibly) if no hybrid scenarios
are found after filtering.
}
\description{
For hybrid scenarios (those with \code{n_new_sites > 0}), shows both the
combined (legacy + new sites) and the legacy-only power curves on the same
panel.  The vertical gap between the solid and dashed lines at any effect
size is the power gain attributable solely to the new sites.

Non-hybrid scenarios are silently dropped unless \code{include_legacy_only} is
\code{TRUE}, in which case they appear as a single solid line (combined =
legacy-only).
}
\seealso{
\code{\link[=plot_power]{plot_power()}}, \code{\link[=run_power_sim]{run_power_sim()}}
}
