% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summaries.R
\name{extend}
\alias{extend}
\title{Extend power results with additional iterations}
\usage{
extend(results, additional, alpha = NULL)
}
\arguments{
\item{results}{A \code{monpwr_results} data frame.}

\item{additional}{A \code{monpwr_results} data frame with the same structure
(typically from a second \code{\link[=run_power_sim]{run_power_sim()}} call with different seeds).}

\item{alpha}{Numeric scalar.  Significance threshold for the re-summary.
Default uses \code{attr(results, "alpha")}, falling back to 0.10.}
}
\value{
A \code{monpwr_results} data frame with pooled p-values and updated
power estimates.
}
\description{
Combines two \code{monpwr_results} data frames by concatenating their raw
p-values for matching cells (same scenario × group × effect × horizon),
then recomputes power and CIs from the pooled replicates.  This lets you
accumulate iterations incrementally rather than re-running from scratch.

Both inputs must have a \code{p_values} list-column.  Cells present in only
one input are kept as-is.
}
\seealso{
\code{\link[=run_power_sim]{run_power_sim()}}, \code{\link[=retest]{retest()}}
}
