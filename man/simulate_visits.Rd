% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_visits}
\alias{simulate_visits}
\title{Simulate future visits for a set of plots}
\usage{
simulate_visits(plot_state, n_future, eff_log, ref_params, draw_re = FALSE)
}
\arguments{
\item{plot_state}{Data frame, one row per plot.  Must contain:
\code{plotid} (character), \code{visit_num} (integer), \code{eta_last_cond} (numeric),
\code{eta_last_zi} (numeric).  Produced by \code{\link[=init_conditional]{init_conditional()}},
\code{\link[=init_prospective_marginal]{init_prospective_marginal()}}, or a custom \code{init_fn}.}

\item{n_future}{Integer scalar.  Number of additional visits to simulate
beyond each plot's current \code{visit_num}.}

\item{eff_log}{Numeric scalar.  The hypothetical effect size on the
log scale: \code{log(1 + effect_pct / 100)}.  This is the trend coefficient
applied to future visits.}

\item{ref_params}{A \code{monpwr_params} object from \code{\link[=extract_params]{extract_params()}}.}

\item{draw_re}{Logical.  If \code{TRUE} (default for prospective mode), fresh
random effects are drawn from \code{N(0, sigma_cond)} and \code{N(0, sigma_zi)}
for each plot.  If \code{FALSE} (conditional mode), random effects are already
absorbed into \code{eta_last_cond} / \code{eta_last_zi} and are not re-drawn.}
}
\value{
A data frame with one row per plot × future visit, containing:
\code{plotid}, \code{visit_num}, \code{log_effort} (\code{ref_params$log_effort_future} —
median observed effort, or \code{0} if no offset), \code{count}, \code{source} (\code{"future"}).
}
\description{
The core simulation engine.  Given a plot-state data frame (from either
\code{\link[=init_conditional]{init_conditional()}} or \code{\link[=init_prospective_marginal]{init_prospective_marginal()}} / a custom \code{init_fn}),
simulates \code{n_future} additional visits per plot under a specified effect size,
using the parameters in \code{ref_params}.

Counts are drawn from the appropriate distribution for the model family
(\code{ref_params$family}):
\itemize{
\item \code{"hurdle_nbinom2"} — truncated NB2 for occupied plots, Bernoulli for occupancy
\item \code{"nbinom2"} — NB2 (including zeros)
\item \code{"poisson"} — Poisson
\item \code{"binomial"} — Bernoulli (occupancy only; count = 0 or 1)
\item \code{"gaussian"} — Normal with SD = \code{ref_params$disp_par}
}
}
\seealso{
\code{\link[=build_historical]{build_historical()}}, \code{\link[=fit_and_test]{fit_and_test()}}, \code{\link[=run_power_sim]{run_power_sim()}}
}
