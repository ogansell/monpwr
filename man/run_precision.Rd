% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summaries.R
\name{run_precision}
\alias{run_precision}
\title{Run precision estimation across scenarios}
\usage{
run_precision(
  scenarios,
  data,
  plot_metadata,
  n_boot = 1000L,
  reporting_groups = list(),
  n_min_group = 5L,
  place_var = "Place",
  ...
)
}
\arguments{
\item{scenarios}{Named list of \code{monpwr_scenario} objects from \code{\link[=scenario]{scenario()}}.}

\item{data}{A data frame — the full modelling dataset.}

\item{plot_metadata}{A data frame with one row per legacy site.}

\item{n_boot}{Integer scalar.  Bootstrap replicates.  Default 1000.}

\item{reporting_groups}{Named list mapping display labels to column names
in \code{plot_metadata} — the same list passed to \code{\link[=run_power_sim]{run_power_sim()}}.
Default \code{list()} (overall scale only).}

\item{n_min_group}{Integer scalar.  Minimum retained sites for a group-level
CV.  Default 5.}

\item{place_var}{Character scalar.  Site ID column.  Default \code{"Place"}.}

\item{...}{Passed to \code{\link[=bootstrap_cv]{bootstrap_cv()}}.}
}
\value{
A data frame with columns \code{scenario}, \code{label}, \code{scale}, \code{group},
\code{n_sites}, \code{mean_resp}, \code{cv}.
}
\description{
Convenience wrapper that calls \code{\link[=bootstrap_cv]{bootstrap_cv()}} for each scenario ×
reporting scale combination and returns a combined data frame suitable for
\code{\link[=plot_cv]{plot_cv()}}.

Only \strong{legacy} sites contribute to CV estimates.  New sites in hybrid
scenarios are excluded because they have no observed historical record.
}
\seealso{
\code{\link[=bootstrap_cv]{bootstrap_cv()}}, \code{\link[=plot_cv]{plot_cv()}}
}
